# 更新日志（Changelog）

本项目所有值得记录的变更按时间倒序排列，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 风格。
版本格式：`主版本.次版本.修订`；日期为 `YYYY-MM-DD`。

---

## [v1.1.0] - 2026-09-24

### 新增
- **每日自动归档与模型重训定时任务**
  - 新增可独立运行的 `daily_job.py` 脚本，作为定时任务统一入口。
  - 每日自动执行闭环：初始化扩展表 → 拉取 5 个演示茶园今日 7 天预报并归档进 `weather_history` → 历史不足的茶园生成数据打底 → 用全部茶园历史混合重训 LightGBM 分类器并保存 → 加载 LinUCB bandit 状态。
  - 配置系统级定时任务（每天 05:30，本地环境），使时序/ML 分类/bandit 三引擎每日获取最新气象样本并更新模型。

### 变更
- 归档与重训由「页面访问时懒触发」升级为「定时自动运行」，降低对人工访问的依赖，模型更新更稳定。

### 说明
- 三引擎联动（ML 分类 + 时序 + Contextual Bandit）相关代码与模型文件当前处于**工作区未提交**状态（`git status` 中为新增/已修改），建议后续提交固化。

---

## [v1.0.0] - 2026-09-24

### 新增（三引擎智能风险预警联动）
在规则引擎之上新增联动升级，用于更准确的风险预警与推送择优：

- **决策引擎 `src/bandit.py`**：Contextual Bandit（LinUCB），以环境因子 + 风险概率为 context，在 6 类处置动作（补水灌溉/遮阳降温/防冻保暖/排水防渍/喷药防治/暂缓作业）中在线择优，用反馈（是否采纳、风险是否成真）持续更新；无反馈时未试臂给最大探索分保证每类动作曝光。
- **时序引擎 `src/ts.py`**：`historical_window` 提取近 7 天气象窗口统计特征与趋势；`forecast` 用 statsforecast 的 AutoARIMA 对最高温、降水做未来数日预测，刻画风险指标演化趋势。
- **ML 分类引擎 `src/ml_risk.py`**：LightGBM 多分类器，输入「气象特征 + 茶园环境因子（海拔/品种/月份）」，逐日输出风险等级 {low, medium, high} 概率；冷启动阶段用规则引擎对历史气象弱标注，数据积累后可自动重训；持久化到 `data/models/ml_risk.joblib`。
- **编排中枢 `src/ensemble.py`**：串联「数据打底 → 模型就绪 → 三引擎融合（规则兜底 + ML 概率 + 时序趋势）→ bandit 择优 → 反馈闭环」，融合等级取规则与 ML 中更高者（保守预警），并输出具体处置建议。
- **数据打底层 `src/db.py` 扩展**：新增 `weather_history`（逐日气象归档，按 茶园+日期 UPSERT 去重）、`risk_feedback`（推送反馈样本）两张表。
- **模拟历史打底 `seed_history`**：首次访问为每个茶园自动生成 180 天模拟历史（含高温/低温/暴雨/干旱事件），使管道可演示、风险标签三类齐全。
- **页面接入**：`2_风险预警` 顶部「智能融合研判」、`4_农事建议` 顶部「今日智能推荐」均新增反馈按钮，形成「推送→反馈→回写 bandit + 落库重训」闭环。

### 变更
- `README.md` 新增「三引擎智能风险预警」章节与架构说明。
- `requirements.txt` 新增依赖：`scikit-learn`、`lightgbm`、`statsforecast`、`joblib`。

### 新增依赖
| 包 | 用途 |
| --- | --- |
| `scikit-learn` | 特征标准化 / 训练 Pipeline |
| `lightgbm` | 风险等级多分类 |
| `statsforecast` | AutoARIMA 时序预测 |
| `joblib` | 模型持久化 |

### 冷启动兜底
- ML 未训练 / 时序不可用 / bandit 无反馈时，均安全降级到规则引擎，保证任何情况可用。

---

## [v0.1.0] - 2026-09-19

### 新增（初始版本：茶园气象服务系统）
- **用户系统**：注册 / 登录 / 退出，密码加盐哈希存储（PBKDF2），游客模式可免登录浏览演示茶园。
- **个人茶园**：登录后可添加、编辑、删除自己的茶园，天气与风险分析围绕个人茶园展开。
- **气象监测**：实时天气、空气质量（AQI/PM2.5）、未来 7 天预报、生活指数，附趋势图表。
- **风险预警**：规则引擎研判气象灾害（低温冻害、高温热害、暴雨渍害、大风、干旱）与茶树病虫害（茶小绿叶蝉、茶饼病、炭疽病、茶蚜、尺蠖等），支持调用大模型综合研判。
- **分级机制**：风险按 低/中/高 归档到 持续观察/提前预防/紧急处置 三档，整园预警档位取最高风险，按档位给出处置动作与预案。
- **智能问答**：携带当前气象与风险上下文的农事大模型对话，解答施肥、采摘、打药、灾害防范等问题。
- **农事建议**：基于天气与物候的日常农事操作参考（浇水、施肥、打药、采摘），按类别筛选并可看历史。
- **多茶园**：内置 5 个产茶区茶园配置（龙井、碧螺春、武夷岩茶、铁观音、信阳毛尖）。
- **演示模式**：未配置 API Key 时自动使用内置演示数据，便于快速体验界面。
- **CI 工作流**：`.gitea/workflows/python-ci.yml`，含多次路径修正。

### 技术栈
- Python + Streamlit 多页面前端；和风天气 API；百度千帆（伏羲/文心）大模型；MySQL 数据库；本地规则引擎（离线可用）。

---

## 待办 / 建议
- 将三引擎联动代码与每日任务脚本提交到版本库，固化 `v1.1.0` 变更。
- 积累真实历史数据后，评估分类器在真实样本上的表现（当前以模拟历史 + 规则弱标注训练）。
- 可扩展：定时任务顺带执行一次当日风险研判并落库 `analysis_log`。
